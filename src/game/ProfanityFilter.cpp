// Defunct: ProfanityFilter -- empty TU; v1.6.1 global.constructors.keyed.to.ProfanityFilter.cpp @ 0x00146f70
// (address confirmed correct -- it is the keyed static-init ctor, not a class method).
// ProfanityFilter.cpp was compiled into the binary but emits only a _GLOBAL__I_ static-init
// constructor; zero ProfanityFilter symbols, no vtable exist. The class is header-inlined
// or compiled out; no callers reference it.
