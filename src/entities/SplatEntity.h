#ifndef FN_SPLAT_ENTITY_H
#define FN_SPLAT_ENTITY_H

// Stub — SplatEntity : Entity (type 2)
// Juice splat on background after fruit slice
// Binary: LoadContent 0x173114, Draw 0x17318c
struct SplatEntity {
    static void LoadContent() {} // TODO
};

#endif
